<div class="row w-100 justify-content-center mt-5">
  <div class="col-md-8 col-lg-6">
    <div class="glass-card p-5">
      <a href="/challenge/{{challenge.id}}" class="text-white-50 text-decoration-none small mb-4 d-inline-block">
        <i class="fa-solid fa-arrow-left me-1"></i> Back to Challenge
      </a>
      <h2 class="fw-bold mb-4 text-center">Edit Challenge</h2>

      <form action="/challenge/{{challenge.id}}/edit" method="POST">

        <div class="mb-3">
          <label class="form-label text-white-50 small text-uppercase fw-bold">Challenge Type</label>
          <div class="row g-2" id="type-selector">
            <div class="col-6 col-md-3">
              <input type="radio" class="btn-check" name="challenge_type" id="type_daily" value="daily" autocomplete="off" {{#if (eq challenge.challenge_type 'daily')}}checked{{/if}}>
              <label class="btn w-100 type-btn rounded-3 py-3 d-flex flex-column align-items-center gap-1" for="type_daily" style="background: rgba(255,255,255,0.03); border: 1px solid rgba(255,255,255,0.1); color: white; cursor: pointer;">
                <i class="fa-solid fa-sun fs-4" style="color: var(--primary);"></i>
                <span class="fw-bold small">Daily</span>
                <span class="text-white-50" style="font-size:0.7rem;">1 day</span>
              </label>
            </div>
            <div class="col-6 col-md-3">
              <input type="radio" class="btn-check" name="challenge_type" id="type_weekly" value="weekly" autocomplete="off" {{#if (eq challenge.challenge_type 'weekly')}}checked{{/if}}>
              <label class="btn w-100 type-btn rounded-3 py-3 d-flex flex-column align-items-center gap-1" for="type_weekly" style="background: rgba(255,255,255,0.03); border: 1px solid rgba(255,255,255,0.1); color: white; cursor: pointer;">
                <i class="fa-solid fa-calendar-week fs-4" style="color: var(--primary);"></i>
                <span class="fw-bold small">Weekly</span>
                <span class="text-white-50" style="font-size:0.7rem;">7 days</span>
              </label>
            </div>
            <div class="col-6 col-md-3">
              <input type="radio" class="btn-check" name="challenge_type" id="type_monthly" value="monthly" autocomplete="off" {{#if (eq challenge.challenge_type 'monthly')}}checked{{/if}}>
              <label class="btn w-100 type-btn rounded-3 py-3 d-flex flex-column align-items-center gap-1" for="type_monthly" style="background: rgba(255,255,255,0.03); border: 1px solid rgba(255,255,255,0.1); color: white; cursor: pointer;">
                <i class="fa-solid fa-calendar fs-4" style="color: var(--primary);"></i>
                <span class="fw-bold small">Monthly</span>
                <span class="text-white-50" style="font-size:0.7rem;">~30 days</span>
              </label>
            </div>
            <div class="col-6 col-md-3">
              <input type="radio" class="btn-check" name="challenge_type" id="type_group" value="group" autocomplete="off" {{#if (eq challenge.challenge_type 'group')}}checked{{/if}}>
              <label class="btn w-100 type-btn rounded-3 py-3 d-flex flex-column align-items-center gap-1" for="type_group" style="background: rgba(255,255,255,0.03); border: 1px solid rgba(255,255,255,0.1); color: white; cursor: pointer;">
                <i class="fa-solid fa-users fs-4" style="color: var(--secondary);"></i>
                <span class="fw-bold small">Group</span>
                <span class="text-white-50" style="font-size:0.7rem;">Custom</span>
              </label>
            </div>
          </div>
        </div>

        <div class="mb-3">
          <label class="form-label text-white-50 small text-uppercase fw-bold">Category</label>
          <select class="form-control bg-dark text-white border-secondary" name="category" required>
            <option value="Fitness" {{#if (eq challenge.category 'Fitness')}}selected{{/if}}>Fitness</option>
            <option value="Productivity" {{#if (eq challenge.category 'Productivity')}}selected{{/if}}>Productivity</option>
            <option value="Educational" {{#if (eq challenge.category 'Educational')}}selected{{/if}}>Educational</option>
          </select>
        </div>

        <div class="mb-3">
          <label class="form-label text-white-50 small text-uppercase fw-bold">Title</label>
          <input type="text" class="form-control" name="title" required value="{{challenge.title}}">
        </div>

        <div class="mb-3">
          <label class="form-label text-white-50 small text-uppercase fw-bold">Description</label>
          <textarea class="form-control" name="description" rows="3">{{challenge.description}}</textarea>
        </div>

        <div class="mb-3">
          <label class="form-label text-white-50 small text-uppercase fw-bold">Entry Type</label>
          <select class="form-control bg-dark text-white border-secondary" name="entry_type" required>
            <option value="checkbox" {{#if (eq challenge.entry_type 'checkbox')}}selected{{/if}}>Checkbox (done / not done)</option>
            <option value="amount" {{#if (eq challenge.entry_type 'amount')}}selected{{/if}}>Amount (track a quantity)</option>
          </select>
        </div>

        <div class="mb-3">
          <label class="form-label text-white-50 small text-uppercase fw-bold">Daily Target</label>
          <input type="number" step="0.01" class="form-control" name="daily_target" value="{{challenge.daily_target}}" required>
        </div>

        <div class="row g-3 mb-4">
          <div class="col-6">
            <label class="form-label text-white-50 small text-uppercase fw-bold">Start Date</label>
            <input type="date" class="form-control" name="start_date" id="start_date" value="{{challenge.start_date}}" required>
          </div>
          <div class="col-6">
            <label class="form-label text-white-50 small text-uppercase fw-bold">End Date</label>
            <input type="date" class="form-control" name="end_date" id="end_date" value="{{challenge.end_date}}" required>
          </div>
        </div>

        <div id="date-hint" class="text-white-50 small mb-4 text-center" style="display:none;"></div>

        <div class="mb-4 p-4 rounded-4" style="background: rgba(255,255,255,0.03); border: 1px solid rgba(255,255,255,0.08);">
          <div class="d-flex align-items-start justify-content-between gap-3">
            <div>
              <p class="fw-bold mb-1 {{#if judgingLocked}}text-white-50{{else}}text-white{{/if}}">
                <i class="fa-solid fa-gavel me-2" style="color: var(--secondary);"></i>Enable Judging
              </p>
              {{#if judgingLocked}}
              <p class="small mb-0" style="color:#c084fc;">Judging cannot be changed once a challenge has started.</p>
              {{else}}
              <p class="text-white-50 small mb-0">Participants must upload a photo with every log entry. Only approved entries count toward the leaderboard.</p>
              {{/if}}
            </div>
            <div class="form-check form-switch flex-shrink-0 mt-1">
              <input class="form-check-input" type="checkbox" role="switch" id="enable_judging" name="enable_judging"
                style="width: 3rem; height: 1.5rem; cursor: {{#if judgingLocked}}not-allowed{{else}}pointer{{/if}};"
                {{#if challenge.enable_judging}}checked{{/if}}
                {{#if judgingLocked}}disabled{{/if}}>
            </div>
          </div>
        </div>

        <button type="submit" class="btn btn-neon w-100">Save Changes</button>
      </form>

      <div class="text-center mt-4">
        <a href="/challenge/{{challenge.id}}" class="text-white-50 text-decoration-none">Cancel</a>
      </div>
    </div>
  </div>
</div>

<style>
  select, option { background-color: #111 !important; color: white !important; }
  .btn-check:checked + .type-btn {
    border-color: var(--primary) !important;
    background: rgba(202, 255, 0, 0.08) !important;
    box-shadow: 0 0 12px rgba(202, 255, 0, 0.2);
  }
  #type_group:checked + .type-btn {
    border-color: var(--secondary) !important;
    background: rgba(144, 0, 255, 0.08) !important;
    box-shadow: 0 0 12px rgba(144, 0, 255, 0.2);
  }
</style>

<script>
(function() {
  const startInput = document.getElementById('start_date');
  const endInput   = document.getElementById('end_date');
  const hint       = document.getElementById('date-hint');

  function toYMD(d) { return d.toISOString().split('T')[0]; }

  function recalcEnd(type) {
    const start = new Date(startInput.value + 'T00:00:00');
    if (isNaN(start)) return;
    let end = new Date(start);

    if (type === 'daily') {
      endInput.readOnly = true;
      hint.style.display = 'block';
      hint.textContent = 'Daily challenges last exactly 1 day.';
    } else if (type === 'weekly') {
      end.setDate(start.getDate() + 6);
      endInput.readOnly = true;
      hint.style.display = 'block';
      hint.textContent = 'Weekly challenges run for 7 days from your start date.';
    } else if (type === 'monthly') {
      end.setDate(start.getDate() + 29);
      endInput.readOnly = true;
      hint.style.display = 'block';
      hint.textContent = 'Monthly challenges run for 30 days from your start date.';
    } else {
      endInput.readOnly = false;
      hint.style.display = 'block';
      hint.textContent = 'Group challenges have a custom date range — adjust as needed.';
      return;
    }
    endInput.value = toYMD(end);
  }

  function getSelectedType() {
    const checked = document.querySelector('input[name="challenge_type"]:checked');
    return checked ? checked.value : 'weekly';
  }

  document.querySelectorAll('input[name="challenge_type"]').forEach(function(radio) {
    radio.addEventListener('change', function() { recalcEnd(this.value); });
  });

  startInput.addEventListener('change', function() { recalcEnd(getSelectedType()); });
})();
</script>
